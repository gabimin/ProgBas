// location es una variable del objeto window
// cuando una variable es parte de un objeto se le llama atributo o propiedad (ej:location)

var ruta = window.location;

console.log(navigator);

// write es una función del objeto document
// cuando una función es parte de un objeto se le llama método (ej: write)

document.write("Estás en: " + ruta)
